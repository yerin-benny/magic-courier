# 구현 구조

화면: 학교/PIN 인증과 캐릭터 선택 → 지도 → 문제 5개 → 도착. 다음 배송은 도착 화면에서 바로 시작하며, 4배송 후 다음 회차도 바로 시작할 수 있다. 출발지는 대한민국으로 고정한다. 지도에서 여권·학교 랭킹으로 이동한다.

서버가 학생 상태, 활성 배송, 문제 스냅샷, 제출 기록을 소유한다. 클라이언트에는 현재 문제만 전송하며 정답·seed·PIN 해시는 보내지 않는다. 매 제출은 현재 문제 ID와 요청 ID를 포함한다. 동일 요청은 재실행하지 않는다.

`core/`는 순수 유리수·문제·경로·채점 모듈, `server/service.ts`는 공통 트랜잭션 서비스다. `server/store.ts`의 메모리/파일 어댑터와 `functions/src/firestore-store.ts`가 같은 계약을 구현한다. 로컬 저장은 단일 프로세스 개발용이며 운영은 Firebase 모드를 사용한다.

Firestore: students/{uid}(본인 읽기), credentials/{학교+닉네임 hash}(서버 전용), deliverySessions/{id}(서버 전용), schools/{id}(공개 학교 집계), leaderboardAggregates/current(학교별 순위·TOP 10·갱신시각), rateLimits/{hash}(서버 전용), authSessions/{hash}(로컬 전용). 학생의 개인 정보와 배송 상태는 인증된 callable에서만 수정한다. 학생 문서는 PIN을 포함하지 않는다.

문제 인터페이스: {id,type,left,right,answer,difficulty,key,word?}. 계산은 기약분수로, 피연산자 표시는 별도로 보존한다. RNG와 seed를 주입하며 최근 60개 키를 제외한다. 유한 후보군 기반 fallback이 있으므로 무한 루프가 없다.

배송 확정 트랜잭션: 5개 정답 기록 → 별가루/스탬프/기록 저장 → 첫 시도 ≥3, 45초 이상, 서울 기준 일일 25건 미만이면 학교 집계 증가. 동일 세션은 한 번만 확정. 첫 시도 여부는 제출 이력으로만 결정한다.

주간 경계는 Asia/Seoul 월요일 00:00. 각 학교는 주차 키별 카운터를 보관한다. 집계는 매시 갱신, 수동 갱신은 전체 집계 기준 30분 쿨다운이다. 클라이언트는 집계 문서 하나만 읽는다.
