# Cloud Functions 환경 변수. 이 파일을 functions/.env 로 복사해 값을 채운다.
# functions/.env 는 git 에 올리지 않는다.
#
# PIN 해시에 한 번 더 섞는 서버 전용 문자열. 아무 긴 무작위 문자열이면 된다.
# 한번 정하면 바꾸지 않는다 (바꾸면 기존 학생들의 PIN 이 전부 안 맞게 된다).
PIN_PEPPER=
