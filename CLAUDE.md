# 마법 배달부의 세계일주 — CLAUDE.md

이 파일은 모든 세션에서 자동으로 읽힌다. `docs/spec.md`가 유일한 기준 문서이고,
이 파일은 그중 매 작업마다 잊으면 안 되는 규칙을 요약한 것이다.
두 문서가 어긋나면 `docs/spec.md`를 따르되, 어긋난 사실을 먼저 보고한다.

## 목적 (한 줄)

새로운 배송지에 가고 싶어서 분수의 나눗셈을 한 문제 더 풀게 만드는 것.

## 흔들리면 안 되는 원칙

- 게임 요소가 문제 풀이보다 앞서지 않는다. 연출이 길어지거나 꾸미기가 학습에 영향을 주면 설계가 틀린 것이다.
- 기능을 임의로 추가하지 않는다. `docs/spec.md`에 없는 것은 만들기 전에 먼저 묻는다.
- 1부 4장의 수 범위와 3부 검수 보고가 어긋나면 3부(검수 결과)가 최종이다.
  3부 "검수에서 잡아낸 오류"는 전부 이미 확정된 수정 사항이다. 되돌리지 않는다.
- 대상은 초등 6학년이다. Windows 학교 PC와 전자칠판(터치, 물리 키보드 없을 수 있음)에서 돌아가야 한다.
- 코드 주석과 UI 문구는 한국어로 쓴다. 식별자(변수·함수·파일명)는 영어다.

## 기술 스택

- Vite + 바닐라 JavaScript (ES 모듈). TypeScript 아님. React 아님.
- 테스트: Vitest (`npm test`).
- 지금은 프론트엔드만 만든다. Firebase는 STEP 11에서 붙인다.
- 저장은 `src/storage/StorageAdapter.js` 인터페이스 뒤에 숨긴다.
  당분간 `LocalStorageAdapter`만 쓰고, 나중에 `FirestoreAdapter`로 갈아끼운다.
  화면·게임 로직은 어댑터 구현체를 직접 import하지 않고 `src/storage/index.js`만 본다.

## 게임 수 체계

- 배송 1건 = 5문제 = 국가 1개 도착 = 여권 스탬프 1개
- 회차(세계일주 1회) = 배송 4건 = 4개국 = 20문제 = 한 차시
- 여권 1권 = 스탬프 30개 = 8회차 (약 160문제 = 한 단원 분량)
- 여권 2권 = 나머지 30개국 = 16회차 누적
- 특별 배송은 7문제 (뒤에 유형 A, F를 하나씩 덧붙임). 낮은 확률.

## 문제 유형 A~G (spec 1부 4장, 3부 반영 최종값)

아래 조건은 STEP 2(2026-09-16)에서 3부 확정 문항 공간을 재현하며 확정한 값이다.
각 생성기 파일의 RULES 상수와 같다. 1부 본문 수치와 다른 곳은 파일 머리 주석에 이유를 적어 두었다.

| 유형 | 한 줄 정의 | 핵심 조건 | 문항 공간 (3부) |
|---|---|---|---|
| A | 분모가 같은 (진분수)÷(진분수), 나누어떨어짐 | 분모 5~40, 두 분수 모두 기약, 몫 2~8 | 565 (565) |
| B | 분모가 같은 (진분수)÷(진분수), 나누어떨어지지 않음 | 분모 5~25, 나누는 분자 2~12, 두 분수 기약, a<b 허용, 답의 분모 12 이하 | 1142 (1139) |
| C | 분모가 다른 (진분수)÷(진분수) | 기약, 분모 2~25 서로 다름, 답의 분모 39 이하, 답 10 이하, 답 1 배제 | 12824 (12824) |
| D | (자연수)÷(진분수) | n 2~24, 분모 2~30 기약, 나누는 분자는 n의 약수이고 n과 다름, 답 60 이하 자연수 | 430 (430) |
| E | (분수)÷(분수)를 곱셈으로 바꾸기 (역수 빈칸 2개 + 계산 결과) | C와 동일, 나누는 분자 2 이상 | C에서 나누는 분자 1 제외 |
| F | (대분수)÷(진분수) 및 (대분수)÷(대분수) | 자연수부 1~5, 분모 2~12; 나누는 수 자연수부 0~3, 분모 2~12; 답 15 이하, 답의 분모 15 이하, 답 1 배제 | 8028 (8163) |
| G | 역연산: □ × c/d = e/f 에서 □ 구하기 | 답(기약, 분모 12 이하, 3 이하, 1 제외)을 먼저 정하고 곱하는 수(기약 진분수, 분모 2~12)를 곱해 곱(기약 진분수, 분모 39 이하)을 만든다 | 2054 (2101) |

- 생성 방식은 전수 열거 후 무작위 선택이다 (`enumerate()` 캐시). 생성 후 폐기·재생성 루프를 쓰지 않는다.

- 문장제는 20맥락(그룹 1: 답이 반드시 자연수 / 그룹 2: 답이 분수여도 됨). spec 4-3.
- 배송별 유형 구성은 고정 데이터다. 무작위 5개 뽑기를 절대 쓰지 않는다.
  배송1 A,B,D,G,문장제(그룹1) / 배송2 A,C,E,F,문장제(그룹2) / 배송3 B,C,D,G,문장제(그룹1) / 배송4 A,E,F,C,문장제(그룹2)
- 최근 60문항 제외 큐는 유형별이 아니라 전체 기준이며 저장 계층에 남는다.
- 분수 객체는 표시용 분자·분모와 계산용 값을 분리한다. 자동 약분으로 8/22가 4/11로 보이면 안 된다.

## 채점 규칙 3종 (spec 5장)

1. 값은 맞는데 약분이 덜 된 답(6/8)은 오답이 아니다. "거의 다 왔어요. 더 간단히 줄일 수 있어요."로
   재입력을 유도한다. 시도 횟수를 차감하지 않고 첫 시도 정답 자격도 유지한다.
2. 가분수와 대분수는 같은 답이다 (7/3 = 2와 1/3).
3. 답이 자연수면 자연수 칸만 입력해도 정답이다. 분모 칸에 1을 요구하지 않는다.

채점 함수는 정답 / 약분필요 / 오답 세 값을 반환한다. 분모 0 등 비정상 입력은 오답이 아니라 입력 거부다.

## 답 입력 UI 규칙 (spec 6장)

- 자연수 칸, 분자 칸, 분모 칸 세 칸을 항상 노출한다. 대분수 토글 방식 금지.
- 한 칸을 채우면 자동으로 다음 칸으로 포커스 이동 (두 자리 수 입력을 방해하지 않는 방식이어야 한다).
- 화면 안에 자체 숫자 키패드를 둔다. 시스템 키보드가 뜨면 안 된다. 물리 키보드 입력도 동시에 받는다.
- 분모 칸에 0을 입력할 수 없게 막는다.
- 엔터 또는 확인 버튼 하나로 제출한다.

- 구현: `src/ui/components/inputModel.js`(상태, DOM 없음) + `fractionInput.js`(DOM·키패드). 자릿수는 자연수부·분모 2, 분자 3.
  자동 이동은 최대 자릿수에 도달했을 때만. 그 전에는 "다음" 키, "/", 오른쪽 화살표로 넘어간다. (STEP 5 제안, 사용자 확정 대기)
- 개발용 시험 페이지: `npm run dev` 후 `/dev/input.html`. 배포에 포함하지 않는다.

## 오답과 보상 (spec 7장)

- 오답 1회: 힌트 제공 후 재도전. 배송은 유지된다. 실패로 되돌리지 않는다.
- 첫 시도 정답 별가루 3, 재시도 정답 별가루 1.
- 5문제 중 첫 시도 정답 3개 이상이면 "정확 배송 보너스". 못 채워도 개인 기록·스탬프는 정상 적립. 부정적 문구 금지.

## 연출과 표시

- 정답 연출은 0.4초 이내, 연타 시 스킵.
- 국기는 이모지 금지. SVG 자산으로만 표시한다 (Windows에서 두 글자 알파벳으로 깨진다).
- 텍스트(국가명, 수도, 숫자, 버튼 글자)는 모두 코드로 렌더링한다. 이미지 안에 글자를 넣지 않는다.
- 문장 안의 분수도 세로 분수로 그린다. 문장제·유형 G 문항은 `segments`(문자열 + Fraction 조각)를
  `renderSegments`로 렌더링하고, 평문 `text`("3/11")는 로그·테스트에만 쓴다. 화면에 "3/11" 글자를 쓰지 않는다.
- 세계지도는 직접 그리지 않는다. Natural Earth 기반 데이터를 쓰되 출처와 라이선스를 먼저 제안하고 확인받는다.
- 그래픽 자산 파일 경로는 `assets/manifest.js`를 통해서만 참조한다. 코드에 경로를 직접 쓰지 않는다.
- 조사 처리: "8/9을(를)" 표기 금지. 앞 숫자의 한자음 받침 규칙(1과, 2와, 3과, 4와, 5와, 6과, 7과, 8과, 9와)으로 확정한다.

## 디자인 (2026-09-16 사용자 요청: 캐릭터 그림체처럼 아기자기하고 따뜻하게)

- 캐릭터 시트의 그림체를 기준으로 한다: 둥근 실루엣, 종이 질감, 라벤더·앰버·크림·세이지·코랄 8색.
- 글꼴: 제목·버튼·숫자는 Jua, 본문은 Gowun Dodum (둘 다 OFL, `@fontsource` 자체 호스팅). 시스템 글꼴 fallback 은 맑은 고딕.
- 배경은 하늘 시트(`backgrounds.sky`), 내용은 종이 카드(`.card`: 크림 종이 + 3px 테두리 + 아래로 떨어지는 그림자).
- 버튼은 알약 모양에 아래쪽 4px 단색 그림자(눌리는 느낌). 강조는 앰버, 보조는 종이색/하늘색.
- 장식 글리프는 ✦ 만 쓴다. 이모지는 쓰지 않는다.
- 캐릭터 측면 포즈(`characters.side-N`)는 지도와 문제 화면에서 72px, 부드럽게 떠다니는 애니메이션. 소포 그림(`parcels.parcel-N`)은 물건 이름으로 고른다(`parcelFor`).
- 원본 시트를 다시 자르려면 `npm run assets:cut` (Python + Pillow). 자산 전체와 48px 판독은 `/dev/assets.html`.
- 빗자루는 시트가 아니라 상위 폴더의 단계별 낱장 PNG(`1단계 빗자루.png` ~ `5단계 빗자루.png`)를 쓴다.
  시트를 5등분하면 비스듬한 손잡이가 옆 칸으로 넘어가 잘린다 (2026-09-16 사용자 지적).
- manifest 버킷: flags, backgrounds, characters(side-1~4, front-sheet), passport(cover, stamp-1~6), parcels(1~8), brooms(1~5, 2차), items(1~12, 2차).
- 지도: 바다 그라데이션 + 구름, 육지는 종이 오려붙인 느낌(아래 그림자), 경로는 흰 테두리 위 앰버 점선, 마커는 스티커 원.
  viewBox 에 좌우 60·상하 30 여백을 둔다. 경도 ±180 근처 나라(피지·뉴질랜드)의 마커와 이름표가 잘렸다.
- 지도의 캐릭터는 다음 배송지 쪽을 본다. 배송지가 화면 왼쪽이면 `scale(-1 1)` 로 뒤집는다.
  경도가 아니라 투영된 화면 x 로 비교해야 그려진 경로선과 방향이 맞는다. 동물 친구도 같이 반대편으로 간다.
- 아이템 그림(items, parcels) 뒤에는 `--sheet-cream` 판을 깐다. 동물 친구·부엉이처럼 몸통이 원본 시트 배경과
  같은 크림색으로 그려진 것들은 배경을 지울 때 몸통도 지워져서, 같은 색 판이 없으면 구멍이 뚫려 보인다.
- 시작 화면: 처음 온 학생에게는 정면 시트, 이어서 하는 학생에게는 나는 배달부 띠 + 내 기록 카드(우체국·스탬프·회차).
- 출발 지역 선택 화면은 없다 (2026-09-16 삭제). 온보딩은 캐릭터 선택 하나뿐이고,
  우체국 이름은 `postOfficeName(schoolId)` 로 학교 이름에서 만든다 ("해운대초 마법 우체국").
  `isOnboarded` 는 characterId 만 본다. 학교 목록·`shortSchoolName` 은 `src/data/schools.js` 에 있다
  (auth.js 도 여기서 가져다 쓴다). 우체국 이름은 파생값이라 `refreshDerived` 가 문서를 열 때마다 다시 만든다.
- 로그인 화면: "마법 우체국 접수대" 머리 + 학교·닉네임·PIN. 가입과 로그인을 나누지 않는다
  (같은 학교·닉네임·비밀번호면 이어진다고 화면에 적어 둔다). 비밀번호는 숫자만 받고 4칸 점으로 보여 준다.

## 화면 뼈대 (2026-09-16 개편)

- 로그인한 뒤의 화면 위에는 상단 내비(`src/ui/components/topBar.js`)가 고정된다:
  로고 · 탭 4개(세계지도·나의 여권·꾸미기·학교 배송량) · 별가루 잔액 · 캐릭터 · 나가기.
  화면마다 아래에 이동 버튼을 늘어놓지 않는다. 화면 안에 "지도로 가기" 같은 버튼을 다시 만들지 않는다.
- 상단 내비를 숨기는 화면: 시작·로그인·캐릭터 선택(고를 것이 하나뿐), 문제 풀이·배송 성공·회차 완료·새 여권(집중·연출).
- 문제 풀이 화면에는 대신 왼쪽 위에 "지도로 나가기"만 둔다. 나가도 세션이 남아 있어서
  지도의 버튼이 "이어서 배달하기"로 바뀌고 같은 문제부터 이어진다 (수업 중 중단 대비).
- 화면 머리는 `page-head`(제목 + 한 줄 설명 + 오른쪽 `pill` 진행 표시)로 통일한다.
- 문제 풀이 화면도 2단이다 (2026-09-16): 왼쪽 문제 카드, 오른쪽 "나의 답"(입력 칸 + 키패드).
  1366×768 학교 PC 에서 스크롤 없이 한 화면에 들어와야 한다. 세로로 쌓으면 키패드가 접힌다.
  진행도는 칸 다섯 개짜리 띠이고 캐릭터가 그 위를 한 칸씩 건너간다(예전 비행 트랙을 여기에 합쳤다).
  `.screen--problem` 안에서 입력 칸·키패드를 조밀한 치수로 덮어쓴다. 누르는 칸은 44px 아래로 내리지 않는다.
- 지도 화면은 넓은 화면에서 2단이다: 왼쪽 지도+경로 목록, 오른쪽 "다음 목적지" 카드와 큰 출발 버튼.
  900px 아래에서는 한 단으로 접힌다. 아래에 빗자루 성장 카드와 숫자 카드 3개(스탬프·배송·별가루)를 둔다.
- 라우터는 `createRouter(root, { onChange })` 로 화면이 바뀔 때마다 상단 내비를 다시 그린다.

## 국가·문구

- 오스트레일리아로 표기한다. 호주라고 쓰지 않는다.
- 러시아, 튀르키예는 아시아로 분류하고 소개에 두 대륙에 걸쳐 있다는 한 줄을 넣는다.
- 경제 수준이나 발전 정도로 국가를 서열화하는 표현을 넣지 않는다.
- 전국 랭킹에는 학교명만 노출한다. 개인 닉네임 비공개.
- 닉네임에 실명을 써도 된다 (2026-09-16 사용자 요청으로 실명 금지 규칙 제거).
  그만큼 "닉네임은 어디에도 공개하지 않는다"를 반드시 지킨다 — 학교 문서·집계 문서에 닉네임을 넣지 않는다.
  닉네임은 2~8자, 욕설 필터만 남았다. UI 문구는 "별명"이 아니라 "닉네임"으로 쓴다.
  화면에서는 "PIN" 이 아니라 "비밀번호"라고 쓴다 (2026-09-16, 학생들이 PIN 을 헷갈려함). 형식은 숫자 4자리 그대로다.
  코드 식별자(pin, pinHash, isValidPin)는 영어를 유지한다. 비밀번호는 로컬 단계에서도 해시로 저장한다.

## 학교 랭킹과 서버 판정 (spec 9장, STEP 11)

- 학교 랭킹에 영향을 주는 판정은 전부 Cloud Functions 에서 한다. 클라이언트는 "완료했다"를 스스로 올릴 수 없다.
- 반영 조건: 5문제 중 첫 시도 정답 3개 이상 + 배송 소요 45초 이상. 하루 상한은 두지 않는다(2026-09-16 확정).
  상한 장치는 `src/game/ranking.js` 의 `dailyLimit` 에 남아 있다. 숫자를 넣으면 켜진다.
- 규칙 원본은 `src/game/ranking.js` 하나다. `npm run sync:functions` 가 `functions/lib/` 로 복사하고
  `npm run verify:functions` 가 어긋남을 잡는다. functions/lib/ 안의 파일을 직접 고치지 않는다.
- 랭킹 화면은 집계 문서(`aggregates/ranking`) 하나만 읽는다. 학교 문서를 전부 정렬해 읽지 않는다.
- 반영되지 않은 배송에도 개인 기록과 스탬프는 정상 적립된다. 화면 문구에 부정적인 말을 쓰지 않는다.
- Firebase 설정(.env.local)이 없으면 앱은 localStorage 로 그대로 돈다. 랭킹 화면만 안내로 바뀐다.
  연결 순서는 `docs/FIREBASE.md`.

## 별가루 사용처 — 별빛 상점 (spec 7-2, 화면 11)

- 별가루로 꾸미기 아이템 17종(빗자루 5 · 모자 2 · 망토 2 · 가방 2 · 핀 2 · 동물 친구 4)을 산다.
- 전부 외형 전용이다. 능력치·확률·힌트를 파는 물건을 넣지 않는다. 뽑기(무작위 상자)도 쓰지 않는다.
- 누적 별가루(`totals.stardust`)는 기록이라 줄지 않는다. 쓴 양은 `totals.stardustSpent` 에 따로 세고
  잔액 = 누적 - 사용 이다 (`balanceOf`).
- 캐릭터 측면 그림에는 이미 모자·망토·빗자루가 그려져 있다. 그 위에 아이템을 겹쳐 그리지 않는다.
  착용한 물건은 배달부 카드의 옆 칸에 놓고, 동물 친구만 지도에서 옆에 같이 난다.
- 값은 `src/data/decorItems.js` 한 곳에 있다. 40~320, 전부 모으면 1540.

## 미정 항목 (임의로 정하지 말고 제안 후 확인받는다)

- 학교 목록 데이터 출처 (당분간 부산 초등학교 20개 임시 JSON)
- 교사용 화면 범위 (3차)

## 코드 구조 (STEP 6 기준)

- `src/core/` 분수·채점·조사·난수. DOM 없음.
- `src/generators/` 유형 A~G, 문장제, 최근 큐, 배송 구성. DOM 없음.
- `src/game/` 배송 세션(deliverySession.js), 힌트(hints.js), 회차 경로 추첨(route.js), 실행 중 상태(gameState.js). DOM 없음.
- `src/data/countries.js` 60개국(ISO 숫자 코드 포함). `src/ui/map/projection.js` 정거원통도법, `src/ui/map/land.js` 육지 층
  (world-atlas 1:110m + topojson-client, 2026-09-16 사용자 승인). 출처·라이선스는 `docs/THIRD_PARTY.md`.
- 국기는 flag-icons(MIT)에서 60개만 `assets/flags/`로 복사한다 (`npm run assets:flags`). 배포물에 MIT 표기가 남아야 한다.
- 자산은 `assets/manifest.js`의 `getAsset('flag.KR')` 같은 키로만 얻는다. null 이면 호출부가 플레이스홀더를 그린다.
- 여권 로직은 `src/game/passport.js`(30개 × 2권, 완성 순간 판정). 여권 화면·새 여권 수여 화면은 `src/ui/screens/`.
- `src/ui/components/` 입력 컴포넌트, 분수·문항 렌더러. `src/ui/screens/` 화면 하나에 파일 하나.
- `src/app/router.js` 화면 등록·전환. 화면은 `render(root, params, nav)` 이고 정리 함수를 돌려줄 수 있다.
- 게임 규칙은 `src/game/` 에 두고 화면은 표시와 입력만 맡는다. 화면 파일에 채점·보상 계산을 넣지 않는다.
- 계정·저장 (STEP 9): `src/game/auth.js`(login/resumeSession/logout), `src/game/studentDoc.js`(spec 10장 학생 문서, 유형별 통계 A~G+W),
  `src/storage/`(StorageAdapter 인터페이스, LocalStorageAdapter, 테스트용 createMemoryStorage). gameState 는 학생 문서를 품고
  변경 지점마다 `persist()` 로 저장한다. 학생 문서 id 는 `${schoolId}:${닉네임 소문자}`. PIN 은 SHA-256(학교:닉네임:PIN) 해시만 저장.
- 닉네임 규칙·욕설·실명형 패턴은 `src/data/nicknameFilter.js` 에만 둔다. 학교 목록은 `src/data/schools.json`(임시 부산 20개, id 유지).
- 로그인 전(게스트) 상태에서도 gameState 는 동작하지만 저장하지 않는다. 지도·캐릭터·지역 화면은 로그인이 없으면 login 으로 보낸다.
- 개발 모드에서만 `window.__game` 으로 gameState 를 볼 수 있다 (main.js, 빌드 제외).
- Firebase (STEP 11): `src/config/firebase.js`(.env 읽기), `src/storage/FirestoreAdapter.js`,
  `functions/index.js`(loginStudent·startDelivery·completeDelivery·집계 갱신), `firestore.rules`,
  `src/ui/screens/rankingScreen.js`. 배송 세션은 `gameState.openDelivery()` 로 열고
  완료 후 `gameState.reportDelivery(summary)` 로 서버 판정을 받아 온다.
- 꾸미기: `src/data/decorItems.js`(아이템·값), `src/game/shop.js`(사기·착용, DOM 없음),
  `src/ui/components/courierCard.js`(캐릭터 + 착용 물건), `src/ui/screens/shopScreen.js`.
  예전 저장본에 없는 꾸미기 필드는 `ensureDecor` 가 읽을 때 채운다 (gameState.loadStudent).

## 작업 방식

- STEP 단위로 진행한다. 지시받은 STEP 범위만 만들고 완료하면 멈추고 보고한다.
- 버그는 재현 테스트를 먼저 쓰고 실패를 확인한 뒤 고친다.
- 생성기·채점 로직을 바꾸면 `npm test`와 `scripts/` 검증 스크립트를 다시 돌린다.
